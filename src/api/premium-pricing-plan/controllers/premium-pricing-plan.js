'use strict'

/**
 * premium-pricing-plan controller
 */

const { createCoreController } = require('@strapi/strapi').factories

module.exports = createCoreController('api::premium-pricing-plan.premium-pricing-plan')
