{
  "routes": [
    {
      "method": "GET",
      "path": "/insights",
      "handler": "insight.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/insights/count",
      "handler": "insight.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/insights/:id",
      "handler": "insight.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/insights",
      "handler": "insight.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/insights/:id",
      "handler": "insight.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/insights/:id",
      "handler": "insight.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
